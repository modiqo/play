interface:
  display_name: "Play"
  short_description: "Run Plays and manage direct-routing policy"
  default_prompt: "Use $play to run or create a reusable outcome, browse Plays, inspect how one was born, or manage direct-routing policy."

policy:
  allow_implicit_invocation: false
