interface:
  display_name: "Play"
  short_description: "Run, create, and manage reusable Plays"
  default_prompt: "Use $play to run or create a reusable outcome, or list my organizations and Plays."

policy:
  allow_implicit_invocation: true
