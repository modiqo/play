interface:
  display_name: "Play"
  short_description: "Use or create reusable outcome Plays"
  default_prompt: "Use $play to find an existing Play for this outcome or ask before exploring and saving a new one."

policy:
  allow_implicit_invocation: true
