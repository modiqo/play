[project]
name = "modiqo-play-controller"
version = "0.4.39"
description = "Typed runtime for the declarative Play controller"
requires-python = ">=3.10"
dependencies = [
    "jsonschema>=4.23,<5",
    "python-statemachine[yaml]>=3.2,<3.3",
    "PyYAML>=6,<7",
]

[dependency-groups]
dev = ["pyright>=1.1.403,<2"]

[tool.uv]
package = false

[tool.pyright]
include = ["scripts/lib/play", "scripts/bin/play-machine", "scripts/bin/play-bootstrap", "tests"]
extraPaths = ["scripts/lib"]
venvPath = "."
venv = ".venv"

[tool.typos.default.extend-words]
metalness = "metalness"
