#!/usr/bin/env python3
"""Render one Play elicitation contract for a target harness."""

from __future__ import annotations

import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parents[2]
sys.path.insert(0, str(ROOT / "scripts" / "lib"))

from play.elicitation import main


if __name__ == "__main__":
    raise SystemExit(main(ROOT / "references" / "controller" / "prompts.yaml"))
