interface:
  display_name: "Play"
  short_description: "Discover, run, create, and share reusable Plays"
  default_prompt: "Use $play to show my digest, run an exact Play, or create and share a reusable outcome."

policy:
  allow_implicit_invocation: true
