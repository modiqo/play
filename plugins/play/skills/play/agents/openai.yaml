interface:
  display_name: "Play"
  short_description: "Discover, run, create, and share reusable Plays"
  default_prompt: "Use $play to show what’s new, run a Play by recognizable name, create and share a reusable outcome, or show how a Play was born."

policy:
  allow_implicit_invocation: true
